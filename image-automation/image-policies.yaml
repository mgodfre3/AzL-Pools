apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: data-ingestion
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: data-ingestion
  policy:
    semver:
      range: ">=0.1.0"
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: pool-detection
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: pool-detection
  policy:
    semver:
      range: ">=0.1.0"
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: pool-design
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: pool-design
  policy:
    semver:
      range: ">=0.1.0"
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: contact-enrichment
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: contact-enrichment
  policy:
    semver:
      range: ">=0.1.0"
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: dashboard
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: dashboard
  policy:
    semver:
      range: ">=0.1.0"
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: foundry-local
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: foundry-local
  policy:
    semver:
      range: ">=1.0.0"
