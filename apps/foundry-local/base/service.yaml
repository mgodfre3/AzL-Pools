apiVersion: v1
kind: Service
metadata:
  name: foundry-local-svc
  namespace: pool-prospect
spec:
  selector:
    app: foundry-local
  ports:
    - port: 5273
      targetPort: 5273
  type: ClusterIP
